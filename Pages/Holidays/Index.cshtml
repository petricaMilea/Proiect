@page
@model Proiect.Pages.Holidays.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].HolidayName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].Country)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].Details)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Holiday[0].Agency)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Holiday) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HolidayName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Agency.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
